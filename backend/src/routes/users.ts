import type { FastifyZodInstance } from '../types/fastify-zod.js';
import { notImplemented } from './_stub.js';

export async function usersRoutes(app: FastifyZodInstance) {
  notImplemented(app, 'users');
}
