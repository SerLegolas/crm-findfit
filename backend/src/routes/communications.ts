import type { FastifyZodInstance } from '../types/fastify-zod.js';
import { notImplemented } from './_stub.js';

export async function communicationsRoutes(app: FastifyZodInstance) {
  notImplemented(app, 'communications');
}
